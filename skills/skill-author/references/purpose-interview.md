# Purpose Interview — Phase 1 상세

## Q1. 한 문장 목적

**좋은 답변**: "React 컴포넌트를 렌더링하는 테스트를 RTL 관례에 맞춰 작성하도록 Claude를 유도한다."

**약한 답변**: "테스트 도와줌" (너무 광범위 → SKILL이 무엇을 하는지 Claude가 모름)

### 답변 해석

- 한 문장에 "대상·동작·기준"이 없으면 재질문.
- "모든 것을 다 한다"는 답변은 **스킬 2개 이상으로 분리** 제안.

## Q2. 트리거 상황

아래 4축으로 수집:

1. **키워드** — 사용자가 쓸 법한 단어·이모지·약어 (예: "TDD", "테스트 먼저", "red-green")
2. **사용자 의도** — 한 줄 의도문 (예: "새 기능을 안전하게 추가하고 싶다")
3. **파일 패턴** — `*.test.ts`, `src/hooks/**/*.ts` 등 — Claude는 이 정보도 description에 반영 가능
4. **명시 호출** — `/<command>` 슬래시 커맨드와 짝을 이룰지

### 답변 해석

- 키워드 3개 미만 → 더 수집. 너무 적으면 description이 발동 범위를 좁게 잡음.
- 트리거 10개 이상 → 오히려 오발동 위험. 반-트리거(Q3) 강화.

## Q3. 반-트리거

**중요도 높음.** 많은 스킬이 이 질문을 건너뛰어서 오발동합니다.

예:
- `tdd-first`의 반-트리거: "기존 코드 그대로 두고 테스트만 보강" → 그 경우는 `/t` 커맨드 또는 별도 스킬.
- `refactoring-strategy`의 반-트리거: "버그 수정" (동작 변경이 목적) → 일반 수정 워크플로.

### 답변 해석

- 반-트리거를 못 쓰는 경우 → 트리거가 너무 범용일 가능성. 다시 Q1~Q2로.

## Q4. 핵심 원칙

사용자가 "이 스킬이 떴을 때 Claude가 반드시 지켜야 할 규칙" 3~5개를 나열.

좋은 예:
1. 사용자에게 Scope를 먼저 질문한다
2. 공식 문서 링크 없는 라이브러리 조언은 거부한다
3. 테스트 없는 영역은 리팩터링 전에 테스트부터 제안한다

나쁜 예:
1. 좋은 코드를 쓴다 (❌ 측정 불가)
2. TDD를 한다 (❌ 그 자체가 스킬이라 순환)

## Q5. 출력 형태

가능한 카테고리:
- **질문 리스트** (예: Scope Narrowing 프로토콜)
- **워크플로 단계** (예: 5 Phase TDD)
- **코드 변경 제안** (diff 형식)
- **리포트** (예: 리뷰 결과 REVIEW.md)
- **의사결정 가이드** (예: "A vs B 중 어느 패턴")

### 답변 해석

- 카테고리 불명확 → SKILL.md 포맷 결정이 어려움. 구체 예시 요청.

## Q6. 참조 자료

SKILL.md에 **항상 로드되면 비용**인 긴 자료:
- 라이브러리 공식 안티패턴 링크 모음 → `references/library-anti-patterns.md`
- 체크리스트 (20 항목 이상) → `references/checklist.md`
- 긴 예시 코드 → `references/examples/`

### 답변 해석

- "SKILL.md 본문에 다 쓸 것" → 150줄 넘으면 반드시 분리. 토큰 낭비.
- "참조 자료 없음" → OK. 짧은 스킬은 SKILL.md 하나로 충분.

## 인터뷰 후 체크

답변 6개가 모이면 사용자에게 **요약 카드**를 제시:

```
=== 스킬 요약 (초안) ===
이름: <name>
목적: <Q1>
트리거: <Q2 키워드 3~5개>
반-트리거: <Q3>
핵심 원칙: <Q4 3~5개>
출력: <Q5>
참조: <Q6 목록>

이대로 Phase 2(트리거 초안)로 진행해도 될까요?
```

승인받은 후에만 Phase 2로.
